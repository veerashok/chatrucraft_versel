fastapi==0.115.2
uvicorn==0.30.5
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

psycopg2-binary==2.9.10      # PostgreSQL connector
SQLAlchemy==2.0.36           # (optional but recommended if you add ORM later)

python-dotenv==1.0.1
email-validator==2.2.0

# Security & reliability (recommended)
passlib[bcrypt]==1.7.4       # if you later store hashed admin passwords
itsdangerous==2.2.0          # secure token utilities
slowapi==0.1.9               # optional rate-limit middleware
